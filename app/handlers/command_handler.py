"""Обработчики команд бота"""

from telegram import Update
from telegram.ext import ContextTypes

from app.utils.logger import setup_logger


logger = setup_logger(__name__)


async def start(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обработчик команды /start"""

    welcome_text = """
   Добро пожаловать в бота для форматирования Word документов по ГОСТ!

   Просто отправьте мне файл формата .docx, и я отформатирую его по стандартам ГОСТ.

✨ Что умеет бот:
• Настройка полей и шрифтов по ГОСТ
• Форматирование заголовков и абзацев
• Правильное оформление списков
• Нумерация страниц
• Обработка таблиц и рисунков
• Форматирование библиографии
• Генерация оглавления

  Отправьте документ, и я начну обработку!
    """

    await update.message.reply_text(welcome_text)
    logger.info(f"Пользователь {update.effective_user.id} запустил бота")


async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обработчик команды /help"""

    help_text = """
📖 Помощь по использованию бота

🔹 Команды:
/start - Начать работу с ботом
/help - Показать эту справку

   Как использовать:
1. Отправьте боту файл .docx
2. Дождитесь обработки (обычно 10-30 секунд)
3. Получите отформатированный документ

   Требования:
• Формат файла: .docx
• Максимальный размер: 50 МБ

   Что форматируется:
• Поля: левое 30мм, правое 10мм, верхнее/нижнее 20мм
• Шрифт: Times New Roman, 14pt
• Междустрочный интервал: 1.5
• Красная строка: 1.25см
• Выравнивание: по ширине

❓ Возникли проблемы?
Убедитесь, что файл имеет расширение .docx и не поврежден.
    """

    await update.message.reply_text(help_text)
    logger.info(f"Пользователь {update.effective_user.id} запросил помощь")