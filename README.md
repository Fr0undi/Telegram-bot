# Telegram Бот для форматирования документов по ГОСТ

Telegram бот для автоматического форматирования документов Word (.docx) в соответствии с требованиями ГОСТ. Отправьте документ боту — получите отформатированный файл за считанные секунды.

## 🚀 Основные возможности

- ✅ Полное форматирование текста по ГОСТ
- ✅ Автоматическая нумерация рисунков и таблиц
- ✅ Исправление типографики (кавычки, тире, пробелы)
- ✅ Форматирование списков с правильными знаками препинания
- ✅ Настройка нумерации страниц (без титульного листа)
- ✅ Обработка заголовков с разрывами страниц
- ✅ Замена сокращений на полные формы
- ✅ Титульный лист остаётся нетронутым

## 📋 Подробное описание возможностей

### 📝 Общее форматирование текста

**Что форматируется:**
- **Шрифт:** Times New Roman, 14pt (для всего документа)
- **Междустрочный интервал:** 1.5
- **Красная строка:** 1.25 см (для обычных абзацев)
- **Выравнивание:** по ширине
- **Отступы:** стандартизированы по ГОСТ

**Пример:**
```
До:                          После:
Arial 12pt               →   Times New Roman 14pt
Интервал 1.0             →   Интервал 1.5
Влево                    →   По ширине
Без красной строки       →   Красная строка 1.25 см
```

### 📄 Нумерация страниц

**Как работает:**
- ✅ Нумерация размещается **снизу по центру**
- ✅ Титульный лист считается **страницей 1**, но **номер не отображается**
- ✅ Нумерация начинается со 2-й страницы (с цифры "2")
- ✅ Если нумерация была сверху — автоматически переносится вниз
- ✅ Старые номера страниц удаляются

**Определение титульного листа:**
Бот ищет конец первой страницы по приоритету:
1. Разрыв страницы (Page Break)
2. Разрыв секции (Section Break)
3. Заголовок "СОДЕРЖАНИЕ" или "ОГЛАВЛЕНИЕ"
4. Главные заголовки (ВВЕДЕНИЕ, ГЛАВА N)

### 📑 Заголовки

**Главные заголовки (16pt, жирный, по центру, с новой страницы):**
- СОДЕРЖАНИЕ
- ОГЛАВЛЕНИЕ
- ВВЕДЕНИЕ
- ЗАКЛЮЧЕНИЕ
- ГЛАВА 1, ГЛАВА 2, ... ГЛАВА N
- СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ
- БИБЛИОГРАФИЧЕСКИЙ СПИСОК
- АННОТАЦИЯ
- РЕФЕРАТ
- ПРИЛОЖЕНИЕ / ПРИЛОЖЕНИЯ

**Подзаголовки (14pt, жирный, по центру):**
- 1.1, 1.2, 1.3 (подразделы)
- 2.1, 2.2, 2.3 (подразделы)

**Что делает бот:**
- ✅ Добавляет разрыв страницы перед главными заголовками
- ✅ Удаляет пустые строки до и после заголовков
- ✅ Форматирует шрифт и выравнивание
- ✅ Убирает нумерацию списка из подзаголовков (если была)

### 📊 Списки

**Нумерованные списки:**
- Шрифт: Times New Roman 14pt (как у основного текста)
- Выравнивание: по ширине
- Красная строка: 1.25 см
- **Автоматическая расстановка знаков препинания:**
  - `;` (точка с запятой) — между элементами
  - `.` (точка) — в конце последнего элемента
  - Строки с двоеточием не изменяются (вводные фразы)

**Маркированные списки:**
- Маркеры сохраняются без изменений
- Шрифт: Times New Roman 14pt
- Выравнивание: по ширине

**Пример преобразования:**
```
До:                              После:
1. Первый пункт                  1. Первый пункт;
2. Второй пункт                  2. Второй пункт;
3. Третий пункт                  3. Третий пункт.
```

### 🖼️ Рисунки

**Автоматическая обработка:**
- ✅ Выравнивание рисунков **по центру**
- ✅ **Автоматическая нумерация** (Рисунок 1, Рисунок 2, ...)
- ✅ Добавление подписи, если отсутствует
- ✅ Исправление существующих подписей

**Формат подписи:**
```
                    [Рисунок по центру]
                 Рисунок 1 – Название рисунка
```

**Что делает бот:**
- Находит все изображения в документе
- Проверяет наличие подписи под рисунком
- Если подписи нет → добавляет "Рисунок N – " (название пользователь добавит сам)
- Если подпись есть → исправляет номер и формат

### 📋 Таблицы

**Автоматическая обработка:**
- ✅ **Автоматическая нумерация** таблиц (Таблица 1, Таблица 2, ...)
- ✅ Добавление подписи, если отсутствует
- ✅ Выравнивание таблицы **по центру страницы**
- ✅ Форматирование содержимого: Times New Roman 14pt

**Формат подписи (по ГОСТ):**
```
                                                                 Таблица 1
                                    Название
                        [Таблица по центру страницы]
```

**Что делает бот:**
1. Находит все таблицы после титульного листа
2. Проверяет наличие подписи перед таблицей
3. Если подписи нет:
   - Добавляет "Таблица N" (по правому краю)
   - Добавляет "Название" (по центру, на следующей строке)
4. Выравнивает таблицу по центру
5. Форматирует содержимое ячеек:
   - Times New Roman 14pt
   - Убирает лишние отступы
   - Выравнивание текста в ячейках

**Важно:** После форматирования замените слово "Название" на реальное название таблицы.

### ✨ Типографика

**Кавычки:**
- `"текст"` → `«текст»`
- `"текст"` → `«текст»`
- `„текст"` → `«текст»`
- `'текст'` → `«текст»`

Контекстный анализ: открывающие и закрывающие кавычки определяются автоматически.

**Тире:**
- `—` (длинное тире) → `–` (среднее тире)
- ` - ` (дефис с пробелами) → ` – ` (среднее тире)

**Неразрывные пробелы:**
Добавляются автоматически в следующих случаях:
- После `№`, `§`: "№ 5" → "№ 5"
- После сокращений: "рис. 1" → "рис. 1"
- Перед единицами измерения: "100 кг" → "100 кг"
- Между инициалами: "И. И. Иванов" → "И. И. Иванов"
- В датах: "1 января" → "1 января"
- В годах: "2024 г." → "2024 г."

**Замена сокращений:**
- `т.к.` → `так как`
- `т.е.` → `то есть`
- `и т.д.` → `и так далее`
- `и т.п.` → `и тому подобное`
- `и др.` → `и другие`

**Множественные пробелы:**
- Все двойные/тройные пробелы заменяются на одинарные
- Обрабатываются пробелы между разными runs (если текст разбит на части)

**Двоеточия:**
- Убирается пробел перед двоеточием: "слово :" → "слово:"
- Слово после двоеточия со строчной: "Тема: Слово" → "Тема: слово"
- Аббревиатуры не изменяются: "Использовано: API" → не меняется

### 📚 Библиография

**Форматирование списка литературы:**
- Автоматически определяет раздел "БИБЛИОГРАФИЧЕСКИЙ СПИСОК" или "СПИСОК ЛИТЕРАТУРЫ"
- Форматирует элементы списка:
  - Times New Roman 14pt
  - Выравнивание: по ширине
  - Междустрочный интервал: 1.5
  - Без красной строки
  - Без отступов

### 🛡️ Защита титульного листа

**Что НЕ форматируется:**
- ❌ Первая страница (титульный лист) **полностью пропускается**
- ❌ Все элементы титульника остаются в оригинальном виде
- ❌ Таблицы на титульном листе не обрабатываются

**Как определяется титульный лист:**
Бот автоматически находит конец первой страницы по одному из признаков:
1. Разрыв страницы в документе
2. Заголовок "СОДЕРЖАНИЕ" или "ОГЛАВЛЕНИЕ"
3. Первый главный заголовок (ВВЕДЕНИЕ, ГЛАВА N)

## 📋 Требования к документу

### Формат и размер
- ✅ Формат файла: `.docx` (Microsoft Word)
- ✅ Максимальный размер: **50 МБ**
- ❌ Форматы `.doc`, `.pdf`, `.txt` не поддерживаются

### Структура документа
- ⚠️ **Первая страница ОБЯЗАТЕЛЬНО должна быть титульным листом**
- ✅ Документ может содержать таблицы, рисунки, списки
- ✅ Документ может иметь любое количество страниц

### Состояние документа
- ❌ Документ не должен быть поврежден
- ❌ Документ не должен быть защищен паролем
- ❌ Документ не должен быть в режиме "Только чтение"

## ✅ После форматирования

**Обязательно проверьте:**

1. **Рисунки:**
   - Проверьте все подписи к рисункам
   - Добавьте названия там, где стоит только "Рисунок N – "
   - Убедитесь, что все рисунки по центру

2. **Таблицы:**
   - ⚠️ **ЗАМЕНИТЕ слово "Название" на реальные названия таблиц**
   - Проверьте, что все таблицы пронумерованы
   - Убедитесь, что таблицы по центру

3. **Заголовки:**
   - Проверьте, что все главные разделы начинаются с новой страницы
   - Убедитесь, что подзаголовки правильно определены

4. **Разбиение на страницы:**
   - Проверьте, что разрывы страниц стоят корректно
   - Убедитесь, что нет "висячих" строк

5. **Общее:**
   - Просмотрите весь документ перед сдачей
   - Проверьте нумерацию страниц (должна начинаться со 2-й страницы)

## 🏗️ Архитектура проекта

```
Telegram-bot/
├── app/
│   ├── config/
│   │   └── settings.py          # Конфигурация (ГОСТ параметры, токен)
│   ├── handlers/
│   │   ├── command_handler.py   # Команды /start, /help
│   │   └── document_handler.py  # Обработка загруженных документов
│   ├── services/
│   │   └── formatter.py         # Основной сервис форматирования (1700+ строк)
│   └── utils/
│       ├── logger.py            # Логирование (консоль + файл)
│       └── file_manager.py      # Управление временными файлами
├── logs/                         # Логи приложения (создается автоматически)
├── temp/                         # Временные файлы (создается автоматически)
├── main.py                       # Точка входа в приложение
├── requirements.txt              # Зависимости проекта
├── .env                          # Переменные окружения
├── .env.example                  # Пример .env
├── .gitignore                    # Игнорируемые git файлы
└── README.md                     # Эта документация
```

### Используемые технологии

**Основной стек:**
- `python-telegram-bot 20.7` — асинхронный фреймворк для Telegram Bot API
- `python-docx 1.1.0` — работа с документами Word через OpenXML
- `lxml >= 4.9.0` — парсинг и модификация XML структур
- `python-dotenv 1.0.0` — управление переменными окружения

**Особенности реализации:**
- Прямая работа с XML структурой документа через OpenXML API
- Низкоуровневая модификация элементов (параграфы, runs, таблицы)
- Асинхронная обработка запросов (не блокирует других пользователей)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <url-репозитория>
cd Telegram-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
```

### 3. Активация виртуального окружения

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Откройте `.env` и укажите токен вашего бота:

```env
BOT_TOKEN=ваш_токен_от_BotFather
LOG_LEVEL=INFO
```

#### Как получить токен бота:
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### 6. Запуск бота

```bash
python main.py
```

После успешного запуска вы увидите:
```
==================================================
   Бот успешно запущен!
==================================================
```

## 📱 Использование

### Быстрый старт

1. Найдите вашего бота в Telegram (по имени, которое вы указали в BotFather)
2. Отправьте команду `/start`
3. Отправьте боту файл `.docx`
4. Дождитесь обработки (обычно 10-60 секунд в зависимости от размера файла)
5. Получите отформатированный документ
6. **Проверьте документ** перед сдачей

### Доступные команды

- `/start` — Начать работу с ботом, показать приветствие и возможности
- `/help` — Показать подробную справку по форматированию

### Время обработки

Зависит от размера документа и скорости интернета

**Большие файлы:** Бот предупредит вас, если обработка займёт больше времени.

## ⚙️ Настройки форматирования

Все настройки находятся в `app/config/settings.py`:

```python
# ГОСТ настройки форматирования
GOST_FONT_NAME = "Times New Roman"
GOST_FONT_SIZE = 14                # Основной текст
GOST_LINE_SPACING = 1.5            # Междустрочный интервал
GOST_INDENT_CM = 1.25              # Красная строка

# Поля страницы
GOST_LEFT_MARGIN_CM = 3            # Левое поле 3 см
GOST_RIGHT_MARGIN_CM = 1.5         # Правое поле 1.5 см
GOST_TOP_MARGIN_CM = 2             # Верхнее поле 2 см
GOST_BOTTOM_MARGIN_CM = 2          # Нижнее поле 2 см
```

## 📊 Логирование

### Уровни логирования

Логи сохраняются в двух местах:
- **Консоль:** уровень `INFO` (основные события)
- **Файл `logs/bot.log`:** уровень `DEBUG` (детальная информация)

### Настройка уровня логирования

В `.env` можно изменить уровень:

```env
LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR, CRITICAL
```

### Что логируется

**INFO уровень:**
- Запуск бота
- Получение файла от пользователя
- Этапы обработки документа
- Результат обработки
- Ошибки

**DEBUG уровень:**
- Детали обработки каждого элемента
- Количество исправлений по каждой функции
- XML операции
- Детали ошибок с трассировкой

### Пример логов

```
2024-12-17 10:30:15 - __main__ - INFO - ==================================================
2024-12-17 10:30:15 - __main__ - INFO -   Бот успешно запущен!
2024-12-17 10:30:15 - __main__ - INFO - ==================================================
2024-12-17 10:32:20 - app.handlers.document_handler - INFO - Получен файл document.docx размером 2.34 МБ от пользователя 123456789
2024-12-17 10:32:20 - app.handlers.document_handler - INFO - Начало обработки документа document.docx
2024-12-17 10:32:25 - app.services.formatter - INFO - Титульный лист заканчивается на параграфе 15
2024-12-17 10:32:26 - app.services.formatter - INFO - Обработано таблиц: 3, добавлено подписей: 2
2024-12-17 10:32:27 - app.services.formatter - INFO - Отформатировано подзаголовков: 12
2024-12-17 10:32:28 - app.handlers.document_handler - INFO - Документ успешно обработан и отправлен
```

## 🔧 Устранение неполадок

### Ошибка: "BOT_TOKEN не установлен"

**Причина:** Не указан токен бота в переменных окружения.

**Решение:**
1. Убедитесь, что файл `.env` существует в корне проекта
2. Проверьте, что в `.env` указан токен: `BOT_TOKEN=ваш_токен`
3. Перезапустите бота

### Ошибка при обработке документа

**Причина:** Файл поврежден или неправильный формат.

**Решение:**
1. Проверьте, что файл имеет расширение `.docx` (не `.doc`)
2. Откройте файл в Microsoft Word и пересохраните
3. Убедитесь, что файл не защищён паролем
4. Проверьте размер файла (максимум 50 МБ)

### Бот не отвечает

**Решение:**
1. Проверьте, что бот запущен (команда `python main.py`)
2. Убедитесь в правильности токена
3. Проверьте логи в `logs/bot.log`
4. Убедитесь, что есть подключение к интернету

### Долгая обработка

**Причина:** Большой размер файла или много элементов.

**Решение:**
- Дождитесь окончания обработки (бот покажет прогресс)
- Для файлов > 10 МБ обработка может занять 2-3 минуты
- Проверьте логи для отслеживания прогресса

## 📝 Лицензия

Этот проект создан для учебных целей.

## 📧 Контакты

При возникновении вопросов или проблем:
- Создавайте [Issue](ссылка) в репозитории
- Опишите проблему максимально детально
- Приложите логи из `logs/bot.log` (если возможно)

---

**Сделано для автоматизации рутинной работы с документами по ГОСТ**
